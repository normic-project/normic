{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm --workspace-root install --frozen-lockfile",
  "buildCommand": "pnpm --workspace-root run build",
  "outputDirectory": ".next"
}
